# toepiebotha-landing-page
Static landing page for Toepie Botha.

## Netlify
- Static site; no npm build step required.
- `netlify.toml` sets an empty build command and publish directory `.` at the repo root.
- After changes, commit/push and trigger a Netlify deploy.
